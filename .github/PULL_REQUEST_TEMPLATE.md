## 요약

<!-- 무엇을 바꿨는지 한두 줄로. -->

## 변경 이유

<!-- 왜 필요한 변경인지. 관련 이슈가 있으면 `Closes #12` 형태로 연결한다. -->

## 테스트 계획

- [ ] `cd backend && ./gradlew test`
- [ ] `cd frontend && npm run build`
- [ ] 

## 확인 사항

- [ ] DART / 네이버 응답 형식 변화에 영향받는 코드를 건드렸다면 해당 클라이언트 테스트를 갱신했다
- [ ] DB 스키마를 바꿨다면 Flyway 마이그레이션을 새 버전으로 추가했다 (기존 파일 수정 금지)
- [ ] 시크릿(DART 키, DB 접속 정보, 동기화 토큰)을 소스에 넣지 않았다
